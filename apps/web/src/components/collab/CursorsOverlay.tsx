"use client";
// TODO: subscribe to awareness and render other users' cursors positioned absolutely
// over the canvas using the viewport transform. Left as a stub so the app boots.
export function CursorsOverlay() {
  return null;
}
